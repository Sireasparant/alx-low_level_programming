#include <stdio.h>
/**
 * main - print if the number is positive, zero, or negative
 *
 * Return: Always 0 (Success/correct)
 */
int main(void)
{
	char ch;

	for (ch = 'a'; ch <= 'z'; ch++)
	{
		putchar(ch);
	}
	putchar('\n');
	return (0);
}
